# Operations guide

The maintained guide and account mapping are at [../../docs/operations.md](../../docs/operations.md). Run CLI commands from this app directory or use `npm run operations -- ...` at the repository root.
